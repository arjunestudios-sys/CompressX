{
  "name": "docholder-frontend",
  "version": "1.0.0",
  "description": "Docholder Frontend Static Web Assets",
  "scripts": {
    "build": "npm --prefix .. run build",
    "cap:sync": "npm --prefix .. run cap:sync",
    "cap:open": "npm --prefix .. run cap:open",
    "build:apk": "npm --prefix .. run build:apk",
    "build:apk:release": "npm --prefix .. run build:apk:release"
  }
}
